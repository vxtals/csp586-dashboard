// Label, Chart type...
class Options {
  constructor(options){
    this.options = options;
  }

  getOptions(){
    return this.options;
  }

  setOptions(options){
    this.options = options;
  }
}
